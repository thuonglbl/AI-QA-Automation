"""Per-(user, project) non-secret AI provider configuration storage.

Stores the user's last-approved provider/model selection as a default
suggestion for future threads in the same project. Secrets never live here.
"""
