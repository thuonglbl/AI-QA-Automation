diff --git a/frontend/package.json b/frontend/package.json
index 381ce29..86b36a5 100644
--- a/frontend/package.json
+++ b/frontend/package.json
@@ -16,7 +16,7 @@
     "@radix-ui/react-checkbox": "^1.1.1",
     "@radix-ui/react-label": "^2.1.0",
     "@radix-ui/react-progress": "^1.1.0",
-    "@radix-ui/react-scroll-area": "^1.1.0",
+    "@radix-ui/react-scroll-area": "^1.2.10",
     "@radix-ui/react-separator": "^1.1.0",
     "@radix-ui/react-slot": "^1.1.0",
     "class-variance-authority": "^0.7.0",
@@ -24,16 +24,21 @@
     "lucide-react": "^0.454.0",
     "react": "^18.3.1",
     "react-dom": "^18.3.1",
+    "react-markdown": "^10.1.0",
+    "react-syntax-highlighter": "^16.1.1",
+    "remark-gfm": "^4.0.1",
     "tailwind-merge": "^2.5.4",
     "tailwindcss-animate": "^1.0.7"
   },
   "devDependencies": {
+    "@tailwindcss/typography": "^0.5.19",
     "@testing-library/dom": "^10.4.1",
     "@testing-library/jest-dom": "^6.9.1",
     "@testing-library/react": "^16.3.2",
     "@types/node": "^22.8.0",
     "@types/react": "^18.3.12",
     "@types/react-dom": "^18.3.1",
+    "@types/react-syntax-highlighter": "^15.5.13",
     "@typescript-eslint/eslint-plugin": "^8.58.2",
     "@typescript-eslint/parser": "^8.58.2",
     "@vitejs/plugin-react": "^4.3.3",
diff --git a/frontend/src/components/ChatArea.tsx b/frontend/src/components/ChatArea.tsx
new file mode 100644
index 0000000..f924f58
--- /dev/null
+++ b/frontend/src/components/ChatArea.tsx
@@ -0,0 +1,83 @@
+import React, { useRef, useEffect, useState } from 'react';
+import type { AgentMessage } from '@/types/pipeline';
+import { ChatMessage } from './ChatMessage';
+import { ScrollArea } from '@/components/ui/scroll-area';
+import { cn } from '@/lib/utils';
+import { ArrowDown } from 'lucide-react';
+
+export interface ChatAreaProps {
+  messages: AgentMessage[];
+  className?: string;
+}
+
+export function ChatArea({ messages, className }: ChatAreaProps) {
+  const scrollViewportRef = useRef<HTMLDivElement>(null);
+  const [isScrolledUp, setIsScrolledUp] = useState(false);
+  const [hasNewMessage, setHasNewMessage] = useState(false);
+  const previousMessageCount = useRef(messages?.length || 0);
+
+  useEffect(() => {
+    const isNewMessage = (messages?.length || 0) > previousMessageCount.current;
+    
+    if (isNewMessage) {
+      if (isScrolledUp) {
+        setHasNewMessage(true);
+      } else {
+        scrollToBottom();
+      }
+    } else {
+      if (!isScrolledUp) {
+        scrollToBottom();
+      }
+    }
+    
+    previousMessageCount.current = messages?.length || 0;
+  }, [messages, isScrolledUp]);
+
+  const scrollToBottom = () => {
+    if (scrollViewportRef.current) {
+      scrollViewportRef.current.scrollTop = scrollViewportRef.current.scrollHeight;
+      setHasNewMessage(false);
+    }
+  };
+
+  const handleScroll = (e: React.UIEvent<HTMLDivElement>) => {
+    const target = e.currentTarget;
+    const scrollPosition = target.scrollTop + target.clientHeight;
+    // 20px threshold for determining "at bottom"
+    const isAtBottom = target.scrollHeight - scrollPosition < 20;
+    
+    if (isAtBottom) {
+      setIsScrolledUp(false);
+      setHasNewMessage(false);
+    } else {
+      setIsScrolledUp(true);
+    }
+  };
+
+  return (
+    <div className={cn("relative flex w-full flex-col", className)}>
+      <ScrollArea 
+        className="flex-1 w-full" 
+        viewportRef={scrollViewportRef}
+        onScroll={handleScroll}
+      >
+        <div role="list" className="flex flex-col p-4 w-full h-full pb-10">
+          {(messages || []).map((msg) => (
+            <ChatMessage key={msg.id} message={msg} />
+          ))}
+        </div>
+      </ScrollArea>
+      
+      {hasNewMessage && isScrolledUp && (
+        <button
+          onClick={scrollToBottom}
+          className="absolute bottom-4 left-1/2 -translate-x-1/2 flex items-center gap-2 bg-blue-600 text-white rounded-full px-4 py-1.5 text-xs font-semibold shadow-md active:scale-95 transition-transform hover:bg-blue-700"
+        >
+          <ArrowDown className="w-3 h-3" />
+          New message
+        </button>
+      )}
+    </div>
+  );
+}
diff --git a/frontend/src/components/ChatMessage.tsx b/frontend/src/components/ChatMessage.tsx
new file mode 100644
index 0000000..572bddb
--- /dev/null
+++ b/frontend/src/components/ChatMessage.tsx
@@ -0,0 +1,57 @@
+import React from 'react';
+import { User, Bot } from 'lucide-react';
+import type { AgentMessage } from '@/types/pipeline';
+import { cn } from '@/lib/utils';
+import { ReviewContent } from './ReviewContent';
+
+export interface ChatMessageProps {
+  message: AgentMessage;
+}
+
+export function ChatMessage({ message }: ChatMessageProps) {
+  const isAgent = message.sender === 'agent';
+
+  return (
+    <div 
+      className={cn("flex w-full mb-4", isAgent ? "justify-start" : "justify-end")}
+      role="listitem"
+    >
+      <div className={cn(
+        "flex max-w-[80%] p-4 rounded-lg items-start gap-4 shadow-sm",
+        isAgent 
+          ? "bg-white text-slate-900 border border-slate-200 rounded-bl-none" 
+          : "bg-blue-500 text-white rounded-br-none"
+      )}>
+        {/* Avatar */}
+        <div className={cn(
+          "flex-shrink-0 flex items-center justify-center h-8 w-8 rounded-full border",
+          isAgent 
+            ? "border-slate-200 bg-slate-50 text-slate-500" 
+            : "border-blue-400 bg-blue-600 text-blue-50"
+        )}>
+          {isAgent ? <Bot className="h-5 w-5" /> : <User className="h-5 w-5" />}
+        </div>
+        
+        {/* Content */}
+        <div className="flex flex-col gap-1 w-full min-w-0">
+          <div className="flex items-center gap-2">
+            <span className={cn("text-xs font-semibold", isAgent ? "text-slate-700" : "text-blue-100")}>
+              {isAgent ? message.agentName || 'Agent' : 'User'}
+            </span>
+            <span className={cn("text-[10px]", isAgent ? "text-slate-400" : "text-blue-200")}>
+              {new Date(message.timestamp).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}
+            </span>
+          </div>
+          
+          <div className="text-sm pt-1">
+            {isAgent ? (
+              <ReviewContent content={message.content} />
+            ) : (
+              <p className="whitespace-pre-wrap break-words">{message.content}</p>
+            )}
+          </div>
+        </div>
+      </div>
+    </div>
+  );
+}
diff --git a/frontend/src/components/ReviewContent.tsx b/frontend/src/components/ReviewContent.tsx
new file mode 100644
index 0000000..d744557
--- /dev/null
+++ b/frontend/src/components/ReviewContent.tsx
@@ -0,0 +1,60 @@
+import React from 'react';
+import ReactMarkdown from 'react-markdown';
+import remarkGfm from 'remark-gfm';
+import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter';
+import { vscDarkPlus } from 'react-syntax-highlighter/dist/esm/styles/prism';
+import { ScrollArea } from '@/components/ui/scroll-area';
+import { cn } from '@/lib/utils';
+
+interface ReviewContentProps {
+  content: string;
+  className?: string;
+}
+
+export function ReviewContent({ content, className }: ReviewContentProps) {
+  return (
+    <ScrollArea className={cn("w-full max-h-[400px]", className)}>
+      <ReactMarkdown
+        remarkPlugins={[remarkGfm]}
+        className="prose prose-sm dark:prose-invert max-w-none prose-p:leading-relaxed prose-pre:p-0 my-0 break-words"
+        components={{
+          code({ node, inline, className, children, ...props }: any) {
+            const match = /language-(\w+)/.exec(className || '');
+            return !inline && match ? (
+              <SyntaxHighlighter
+                {...props}
+                style={vscDarkPlus as any}
+                language={match[1]}
+                PreTag="div"
+                className="rounded-md my-4 text-xs !m-0 !mt-2 !mb-2"
+              >
+                {String(children).replace(/\n$/, '')}
+              </SyntaxHighlighter>
+            ) : (
+              <code {...props} className={cn("bg-slate-100 text-slate-800 px-1 py-0.5 rounded text-xs font-mono break-words", className)}>
+                {children}
+              </code>
+            );
+          },
+          table({ children, ...props }: any) {
+            return (
+              <div className="overflow-x-auto my-4 border rounded-md border-slate-200">
+                <table className="w-full text-sm text-left" {...props}>
+                  {children}
+                </table>
+              </div>
+            );
+          },
+          th({ children, ...props }: any) {
+            return <th className="bg-slate-50 px-3 py-2 font-semibold text-slate-700 border-b" {...props}>{children}</th>;
+          },
+          td({ children, ...props }: any) {
+            return <td className="px-3 py-2 border-b border-slate-100" {...props}>{children}</td>;
+          },
+        }}
+      >
+        {content}
+      </ReactMarkdown>
+    </ScrollArea>
+  );
+}
diff --git a/frontend/src/components/__tests__/ChatArea.test.tsx b/frontend/src/components/__tests__/ChatArea.test.tsx
new file mode 100644
index 0000000..8520fd2
--- /dev/null
+++ b/frontend/src/components/__tests__/ChatArea.test.tsx
@@ -0,0 +1,61 @@
+import { render, screen, fireEvent } from '@testing-library/react';
+import { describe, it, expect, vi } from 'vitest';
+import { ChatArea } from '../ChatArea';
+import type { AgentMessage } from '@/types/pipeline';
+import React from 'react';
+
+// Mock scroll functions
+Element.prototype.scrollTo = vi.fn();
+
+describe('ChatArea', () => {
+  const mockMessages: AgentMessage[] = [
+    {
+      id: '1',
+      sender: 'agent',
+      agentName: 'Alice',
+      content: 'Configuring settings...',
+      timestamp: '2026-04-16T10:00:00Z',
+      messageType: 'text'
+    },
+    {
+      id: '2',
+      sender: 'user',
+      content: 'Looks good.',
+      timestamp: '2026-04-16T10:01:00Z',
+      messageType: 'text'
+    }
+  ];
+
+  it('renders a list of chat messages', () => {
+    render(<ChatArea messages={mockMessages} />);
+    
+    // Check for role="list" on container
+    expect(screen.getByRole('list')).toBeInTheDocument();
+    
+    // Check content
+    expect(screen.getByText('Configuring settings...')).toBeInTheDocument();
+    expect(screen.getByText('Looks good.')).toBeInTheDocument();
+  });
+
+  it('shows "Γåô New message" exactly when scrolled up and new messages arrive', () => {
+    const { rerender } = render(<ChatArea messages={[mockMessages[0]]} />);
+    
+    expect(screen.queryByText(/New message/i)).not.toBeInTheDocument();
+    
+    // Simulate scroll up (scrollTop < scrollHeight - clientHeight)
+    const scrollContainer = screen.getByRole('list').parentElement as HTMLElement;
+    
+    // Mock the scroll state to be scrolled up
+    Object.defineProperty(scrollContainer, 'scrollHeight', { value: 1000, configurable: true });
+    Object.defineProperty(scrollContainer, 'clientHeight', { value: 500, configurable: true });
+    Object.defineProperty(scrollContainer, 'scrollTop', { value: 200, configurable: true });
+    
+    fireEvent.scroll(scrollContainer);
+    
+    // Rerender with new message
+    rerender(<ChatArea messages={mockMessages} />);
+    
+    // The "New message" indicator should be visible
+    expect(screen.getByText(/New message/i)).toBeInTheDocument();
+  });
+});
diff --git a/frontend/src/components/__tests__/ChatMessage.test.tsx b/frontend/src/components/__tests__/ChatMessage.test.tsx
new file mode 100644
index 0000000..ade9445
--- /dev/null
+++ b/frontend/src/components/__tests__/ChatMessage.test.tsx
@@ -0,0 +1,54 @@
+import { render, screen } from '@testing-library/react';
+import { describe, it, expect } from 'vitest';
+import { ChatMessage } from '../ChatMessage';
+import type { AgentMessage } from '@/types/pipeline';
+
+describe('ChatMessage', () => {
+  const mockAgentMessage: AgentMessage = {
+    id: '1',
+    sender: 'agent',
+    agentName: 'Alice',
+    content: 'Hello, how can I help you?',
+    timestamp: '2026-04-16T10:00:00Z',
+    messageType: 'text'
+  };
+
+  const mockUserMessage: AgentMessage = {
+    id: '2',
+    sender: 'user',
+    content: 'I have a question.',
+    timestamp: '2026-04-16T10:01:00Z',
+    messageType: 'text'
+  };
+
+  it('renders agent message correctly', () => {
+    const { container } = render(<ChatMessage message={mockAgentMessage} />);
+    
+    // Agent name should be visible
+    expect(screen.getByText('Alice')).toBeInTheDocument();
+    
+    // Content should be visible
+    expect(screen.getByText('Hello, how can I help you?')).toBeInTheDocument();
+    
+    // Verify listitem role
+    expect(screen.getByRole('listitem')).toBeInTheDocument();
+    
+    // Verify layout classes: background white, left-aligned
+    const messageBubble = screen.getByText('Hello, how can I help you?').closest('div')?.parentElement;
+    expect(messageBubble).toHaveClass('bg-white');
+  });
+
+  it('renders user message correctly', () => {
+    const { container } = render(<ChatMessage message={mockUserMessage} />);
+    
+    // Content should be visible
+    expect(screen.getByText('I have a question.')).toBeInTheDocument();
+    
+    // User message should not display generic agent name or should display User
+    expect(screen.queryByText('Alice')).not.toBeInTheDocument();
+    
+    // Verify layout classes: background blue, right-aligned
+    const messageBubble = screen.getByText('I have a question.').closest('div')?.parentElement;
+    expect(messageBubble).toHaveClass('bg-blue-500');
+  });
+});
diff --git a/frontend/src/components/__tests__/ReviewContent.test.tsx b/frontend/src/components/__tests__/ReviewContent.test.tsx
new file mode 100644
index 0000000..ce42662
--- /dev/null
+++ b/frontend/src/components/__tests__/ReviewContent.test.tsx
@@ -0,0 +1,23 @@
+import { render, screen } from '@testing-library/react';
+import { describe, it, expect } from 'vitest';
+import { ReviewContent } from '../ReviewContent';
+import React from 'react';
+
+describe('ReviewContent', () => {
+  it('renders markdown content', () => {
+    render(<ReviewContent content="**Bold text** and *italic*" />);
+    
+    expect(screen.getByText('Bold text')).toHaveTagName('strong');
+    expect(screen.getByText('italic')).toHaveTagName('em');
+  });
+
+  it('renders code blocks correctly', () => {
+    // Basic test to see if standard code block structure handles it
+    // since react-syntax-highlighter is complex to mock/test easily via RTL
+    const { container } = render(<ReviewContent content={`\`\`\`json\n{"foo": "bar"}\n\`\`\``} />);
+    
+    // The rendered output will have syntax highlighter elements, we can just check for text
+    expect(container).toHaveTextContent('"foo"');
+    expect(container).toHaveTextContent('"bar"');
+  });
+});
diff --git a/frontend/src/components/ui/scroll-area.tsx b/frontend/src/components/ui/scroll-area.tsx
new file mode 100644
index 0000000..d7584b0
--- /dev/null
+++ b/frontend/src/components/ui/scroll-area.tsx
@@ -0,0 +1,52 @@
+import * as React from "react"
+import * as ScrollAreaPrimitive from "@radix-ui/react-scroll-area"
+
+import { cn } from "@/lib/utils"
+
+const ScrollArea = React.forwardRef<
+  React.ElementRef<typeof ScrollAreaPrimitive.Root>,
+  React.ComponentPropsWithoutRef<typeof ScrollAreaPrimitive.Root> & {
+    viewportRef?: React.Ref<HTMLDivElement>;
+    onScroll?: React.UIEventHandler<HTMLDivElement>;
+  }
+>(({ className, children, viewportRef, onScroll, ...props }, ref) => (
+  <ScrollAreaPrimitive.Root
+    ref={ref}
+    className={cn("relative overflow-hidden", className)}
+    {...props}
+  >
+    <ScrollAreaPrimitive.Viewport 
+      ref={viewportRef} 
+      onScroll={onScroll} 
+      className="h-full w-full rounded-[inherit]">
+      {children}
+    </ScrollAreaPrimitive.Viewport>
+    <ScrollBar />
+    <ScrollAreaPrimitive.Corner />
+  </ScrollAreaPrimitive.Root>
+))
+ScrollArea.displayName = ScrollAreaPrimitive.Root.displayName
+
+const ScrollBar = React.forwardRef<
+  React.ElementRef<typeof ScrollAreaPrimitive.ScrollAreaScrollbar>,
+  React.ComponentPropsWithoutRef<typeof ScrollAreaPrimitive.ScrollAreaScrollbar>
+>(({ className, orientation = "vertical", ...props }, ref) => (
+  <ScrollAreaPrimitive.ScrollAreaScrollbar
+    ref={ref}
+    orientation={orientation}
+    className={cn(
+      "flex touch-none select-none transition-colors",
+      orientation === "vertical" &&
+        "h-full w-2.5 border-l border-l-transparent p-[1px]",
+      orientation === "horizontal" &&
+        "h-2.5 flex-col border-t border-t-transparent p-[1px]",
+      className
+    )}
+    {...props}
+  >
+    <ScrollAreaPrimitive.ScrollAreaThumb className="relative flex-1 rounded-full bg-border" />
+  </ScrollAreaPrimitive.ScrollAreaScrollbar>
+))
+ScrollBar.displayName = ScrollAreaPrimitive.ScrollAreaScrollbar.displayName
+
+export { ScrollArea, ScrollBar }
diff --git a/frontend/tailwind.config.js b/frontend/tailwind.config.js
index c7a10a2..703ff6c 100644
--- a/frontend/tailwind.config.js
+++ b/frontend/tailwind.config.js
@@ -107,5 +107,8 @@ export default {
       },
     },
   },
-  plugins: [require("tailwindcss-animate")],
+  plugins: [
+    require("tailwindcss-animate"),
+    require("@tailwindcss/typography")
+  ],
 }
